# 获取用户输入的正整数
n = int(input(""))

# 用列表保存每一步的结果
a = []

# 执行 3n + 1 猜想的过程
while n != 1:
    if n % 2 == 0:
        n = n // 2  # 如果是偶数，除以2
    else:
        n = 3 * n + 1  # 如果是奇数，乘3加1
    a.append(n)  # 将每一步的结果加入列表

# 输出结果，每个数字用逗号隔开
print(",".join(map(str, a)))
